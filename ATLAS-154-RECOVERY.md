# Exact 154-value C_k atlas — source recovery record

The estate refers to an exact **154-value** atlas associated with the `C_k` / Pascal-relation extremal program in this repository.

The structural mathematics has been recovered and published here, including the distinction between any-ordering and increasing-only semantics, the `C_3` gap law, exact finite tables, cross-order birth data and related barriers.

The original machine-readable **154-value atlas file itself was not recovered** during the release-day source pass.

## Current authority boundary

- structural theorem/reduction material: public;
- exact values explicitly written in existing public documents: public;
- the complete original 154-value raw atlas: **missing source asset**.

No missing entry should be reconstructed by pattern-matching, interpolation or memory and then presented as the original atlas.

## Recovery rule

When a candidate source file is found:

1. preserve its original bytes;
2. compute and publish SHA-256;
3. compare every value already exposed in this repository;
4. record semantic convention (`distinct/injective`, ordering convention, coefficient family, etc.);
5. quarantine any conflicting versions rather than silently choosing one;
6. then promote the verified machine-readable atlas here.

**Status:** `MISSING_SOURCE_ASSET / RECONSTRUCT_OR_RECOVER_FIRST`.
